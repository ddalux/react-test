# react-ai

Minimal Vite + React "Hello World" created from the Vite guide.

How to run:

```bash
# install deps
npm install

# dev server
npm run dev

# build
npm run build

# preview build
npm run preview
```
